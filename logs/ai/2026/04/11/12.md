## 작업: 공통 기반 셸과 라우트 그룹 골격 구현

### 작업 시각
- 2026-04-11 12:18

### 요청 요약
- `docs/ai/README.md`의 규칙을 따르고 `docs/plan/function/mvp-1/00-common-foundation.md` 명세를 기준으로 공통 기반 기능의 컴포넌트를 안정적으로 구현한다.

### 합의된 범위
- `(auth)`, `(main)` 라우트 그룹과 공통 레이아웃 골격을 만든다.
- 공통 타입, 셸 서비스, `useAppShell()` 훅, 레이아웃 컴포넌트를 분리한다.
- 현재 비어 있는 스타터 화면은 공통 기반 프리뷰 화면으로 교체한다.
- 상위 설계 문서와 충돌하던 프로필 경로 표기는 최소 범위로 문서 동기화한다.

### 참고 문서
- `docs/ai/README.md`
- `docs/ai/prompts/base-agent.md`
- `docs/ai/prompts/logging-rules.md`
- `docs/project-plan.MD`
- `docs/plan/layout/architecture.md`
- `docs/plan/layout/project-structure.md`
- `docs/plan/layout/route-design.md`
- `docs/plan/layout/design-system.md`
- `docs/plan/layout/design-system/components-layout.md`
- `docs/plan/layout/design-system/auth-login.md`
- `docs/plan/function/mvp-1/00-common-foundation.md`
- `docs/plan/function/mvp-1/12-shell-navigation.md`
- `node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md`
- `node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md`
- `node_modules/next/dist/docs/01-app/01-getting-started/11-css.md`
- `node_modules/next/dist/docs/01-app/01-getting-started/13-fonts.md`

### 반영 내용
- `app/layout.tsx`, `app/globals.css`를 서비스용 루트 레이아웃과 토큰 기반 전역 스타일로 교체했다.
- `app/(auth)`, `app/(main)`, `app/(main)/@modal` 구조를 만들고 각 그룹 레이아웃을 분리했다.
- `components/layout`에 `AuthLayout`, `ShellFrame`, `AppHeader`, `BottomNav`, `DesktopSidebar`, `NavIcon`을 추가했다.
- `features/app-shell`에 `MainLayout`, `FoundationScreen`, `useAppShell`, `shellService`를 추가해 상호작용 로직과 표현 계층을 분리했다.
- `types/app-shell.ts`, `lib/utils/cn.ts`를 추가해 공통 계약과 유틸을 분리했다.
- `/`, `/create`, `/p/[shortcode]`, `/u/[username]`, `/login`, `/signup` 프리뷰 페이지를 추가해 공통 기반 구조를 직접 확인할 수 있게 했다.
- `docs/plan/function/mvp-1/00-common-foundation.md`의 프로필 경로 설명을 현재 라우트 설계 기준(`/u/{username}`)에 맞게 최소 수정했다.

### 검증
- `npm run lint`
- `npm run build`

### 결과 또는 기대 효과
- 스타터 상태였던 앱이 공통 셸 구조를 가진 실제 서비스 골격으로 전환되었다.
- 이후 인증, 피드, 작성, 프로필 기능은 기존 셸과 레이아웃 컴포넌트 위에 자연스럽게 얹을 수 있다.
- App Router route group, 동적 세그먼트, 공통 토큰, 클라이언트 셸 상태가 모두 빌드 가능한 상태로 정리되었다.

### 남은 이슈
- 현재 `useAppShell()`은 세션 기능 전 단계이므로 프리뷰용 사용자명 `preview-user`를 사용한다.
- 인증 세션이 구현되면 `useAppShell()`의 사용자명 입력을 실제 세션 소스로 교체해야 한다.
- 셸 네비게이션의 세부 활성 탭 규칙과 뒤로가기 fallback 규칙은 `12-shell-navigation.md` 단계에서 추가 정교화가 필요하다.

---

## 작업: 디자인 시스템 프리뷰 라우트 분리

### 작업 시각
- 2026-04-11 12:40

### 요청 요약
- 디자인 시스템 화면이 메인 홈(`/`)을 차지하면 안 되므로, 메인 홈은 유지하고 디자인 시스템은 `/design-system`으로 분리해 달라는 요청을 반영한다.

### 합의된 범위
- `/`는 기존 `(main)` 홈 페이지를 사용한다.
- 디자인 시스템 프리뷰는 별도 `/design-system` 라우트로 이동한다.
- 변경된 라우트 구조에 맞춰 최소 문서만 동기화한다.

### 참고 문서
- `docs/ai/prompts/base-agent.md`
- `docs/ai/prompts/logging-rules.md`
- `docs/plan/layout/architecture.md`
- `docs/plan/layout/route-design.md`
- `node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md`

### 반영 내용
- 루트 `app/page.tsx`를 제거해 `/`가 다시 `app/(main)/page.tsx`를 메인 홈으로 사용하도록 정리했다.
- `app/(main)/design-system/page.tsx`를 추가해 디자인 시스템 쇼케이스를 `/design-system`에서 보도록 분리했다.
- `docs/plan/layout/route-design.md`에 `/design-system` 라우트를 추가하고 라우트 트리/접근 규칙을 함께 갱신했다.

### 검증
- 아직 실행 전

### 결과 또는 기대 효과
- 메인 홈과 내부 디자인 시스템 프리뷰의 역할이 URL 기준으로 분리된다.
- `/`는 메인 셸과 홈 화면 용도로 유지되고, `/design-system`은 UI 기준 확인 전용 경로가 된다.

### 남은 이슈
- 변경 후 `lint`, `test`, `build` 재검증이 필요하다.

---

## 작업: 디자인 시스템 토큰과 공통 UI 프리미티브 구현

### 작업 시각
- 2026-04-11 12:26

### 요청 요약
- `docs/ai/README.md` 규칙을 따르면서 `docs/plan/layout/design-system.md`와 분리 문서를 기준으로 디자인 시스템을 만든다.
- 다만 `docs/plan/function/mvp-1/00-common-foundation.md`에 연결되는 메인 셸 디자인은 병렬 작업을 위해 이번 범위에서 미룬다.

### 합의된 범위
- 토큰의 1차 소스는 `app/globals.css`에 유지한다.
- 공통 UI는 `components/ui`에 props-only 프리미티브로 추가한다.
- 인증 화면 패턴은 `components/layout/auth-shell.tsx`와 `features/auth/components`에 최소 범위로 반영한다.
- 메인 셸 시각 설계(`ShellFrame`, `AppHeader`, `BottomNav`, `DesktopSidebar`)는 기존 병렬 작업을 존중하고 이번 패스에서 건드리지 않는다.
- 홈 화면은 새 디자인 시스템을 확인할 수 있는 프리뷰로만 교체한다.

### 참고 문서
- `docs/ai/README.md`
- `docs/ai/prompts/base-agent.md`
- `docs/ai/prompts/logging-rules.md`
- `docs/plan/layout/architecture.md`
- `docs/plan/layout/project-structure.md`
- `docs/plan/layout/design-system.md`
- `docs/plan/layout/design-system/components-ui.md`
- `docs/plan/layout/design-system/screen-patterns.md`
- `docs/plan/layout/design-system/auth-login.md`
- `docs/plan/function/mvp-1/00-common-foundation.md`
- `node_modules/next/dist/docs/index.md`
- `node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md`
- `node_modules/next/dist/docs/01-app/01-getting-started/11-css.md`
- `node_modules/next/dist/docs/01-app/01-getting-started/13-fonts.md`
- `node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-client.md`

### 반영 내용
- `app/globals.css`에 디자인 시스템 컬러, semantic, mood, border, shadow, radius, content width 토큰을 정리하고 Tailwind theme 변수와 연결했다.
- `app/layout.tsx`의 폰트 구성을 `Geist + DM Serif Display + JetBrains Mono`로 정리하고 메타데이터 설명을 디자인 시스템 기준으로 맞췄다.
- `lib/utils/cn.ts`를 추가해 `clsx + tailwind-merge` 기반 공통 className 병합 유틸을 만들었다.
- `components/ui`에 `Button`, `IconButton`, `Input`, `Textarea`, `FormField`, `Avatar`, `Badge`, `Card`, `Tabs`, `InlineMessage`, `Divider`, `ImageFallback`, `EmptyState`, `LoadingState`, `ErrorState`를 추가했다.
- `components/layout/auth-shell.tsx`를 추가해 인증 화면용 중앙 정렬 단일 카드 레이아웃 컨테이너를 만들었다.
- `features/auth/components`에 `AuthHeader`, `AuthFormCard`, `AuthSwitchLink`, `LoginForm`, `SignupForm`을 추가해 인증 화면 패턴을 feature 계층으로 분리했다.
- `features/design-system/components/design-system-showcase.tsx`와 `app/page.tsx`를 추가해 토큰, 공통 UI, 인증 패턴, 상태 표현을 한 화면에서 확인할 수 있는 쇼케이스를 구성했다.
- 워크트리에 이미 존재하던 `app/(auth)`, `app/(main)` 및 공통 기반 셸 관련 병렬 작업은 건드리지 않고 그대로 두었다.

### 검증
- `npm test`
- `npm run lint`
- `npm run build`

### 결과 또는 기대 효과
- 디자인 토큰과 공통 UI 프리미티브가 코드로 고정되어 이후 feature 구현 시 직접 색상/간격 하드코딩을 줄일 수 있다.
- 인증 화면은 문서에 맞는 단일 카드 패턴으로 바로 재사용 가능한 기반이 생겼다.
- 홈 화면에서 현재 디자인 시스템 상태를 즉시 확인할 수 있어 이후 세부 feature 디자인 작업의 기준점 역할을 한다.

### 남은 이슈
- 메인 셸의 시각 설계와 상호작용(`00-common-foundation.md` 범위)은 이번 패스에서 의도적으로 제외했다.
- 현재 테스트 스택에는 UI 회귀 테스트가 없어서 컴포넌트 시각 회귀는 수동 확인과 빌드 검증에 의존한다.
- 병렬 작업으로 생성된 `app/(auth)`, `app/(main)`, `types/` 등 워크트리 변경이 함께 존재하므로 후속 통합 시 충돌 여부를 다시 확인해야 한다.

---
