# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json

reviews:
  auto_review:
    enabled: true
    drafts: false
    # Review stacked and non-default target branches too.
    base_branches:
      - ".*"
